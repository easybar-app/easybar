# Inbox Demo Widget

This example publishes representative messages to EasyBar's native shared inbox without requiring
external services.

Use the inbox source actions to add or clear the demo snapshot.

The demo uses order `10000` for its source groups and context actions so installed inbox widgets
appear before it.

Copy the complete `inbox-demo/` directory into the configured widgets directory. The native inbox
must be enabled for the messages to be visible.
